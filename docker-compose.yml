version: '3.8'

services:
  webserver:
    image: ghcr.io/klages/ammotracker-web:c2ed06b
    ports:
      - "8081:80"
    volumes:
      - /volume2/docker/ammotracker:/app/data
    depends_on:
      - scraper-worker

  scraper-worker:
    image: ghcr.io/klages/ammotracker-worker:c2ed06b
    volumes:
      - /volume2/docker/ammotracker:/app/data
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
    restart: always
